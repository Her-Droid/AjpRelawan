<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <corners android:radius="12dp" /> <!-- Radius for rounded corners -->
    <stroke
        android:width="2dp"
        android:color="@color/green" /> <!-- Stroke width and color -->
</shape>
